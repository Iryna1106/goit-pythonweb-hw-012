[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
addopts =
    -ra
    --cov=src
    --cov=main
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
    --cov-fail-under=75
